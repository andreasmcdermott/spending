// import { addCollection, Collection } from '../utils/db';

export default class CategoriesCollection {
  constructor() {
    // super('categories');
  }

  async add() {
    // await addCollection(this.name, { unique: ['id', 'name'], indices: ['id'] });
  }
}
